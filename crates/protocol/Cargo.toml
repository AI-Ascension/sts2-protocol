[package]
name = "sts2-protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Neutral STS2 contract metadata and deterministic serialization"

[dependencies]
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
jsonschema = { version = "=0.52.1", default-features = false }
# Test-only SHA-256 oracle for cross-language RCJ vectors; never part of the wire API.
sha2 = { version = "=0.10.9", default-features = false }

[lints]
workspace = true
